# Vantage AI — Deep Site Audit & Minimax 2.5 Implementation Plan

> **Generated by**: Elite Staff-Level Architect
> **Target Executor**: Minimax 2.5 AI Model
> **Codebase Version**: April 2026

---

## 1. Executive Summary & Audit Findings

Following a rigorous, institutional-grade audit of the Vantage AI application across the entire stack (React Frontend, Node.js Backend, Python Quant Engine, and Firebase Infrastructure), several structural bugs, performance bottlenecks, and UX inconsistencies have been identified. 

### 1.1 Critical Vulnerabilities & Bottlenecks

| ID | Module | Severity | Issue |
|----|--------|----------|-------|
| PERF-01 | `server.js:615` | 🔴 HIGH | **Event Loop Blocking (SSR)**: `fs.readFileSync(indexPath, 'utf-8')` is called synchronously on *every single incoming GET request*, blocking the Node.js event loop under heavy load and causing severe latency spikes. |
| LOGIC-01 | `TicketWizard.tsx:69` | 🟠 MED | **Non-Deterministic Ticket Generation**: The `findBestCombination` function uses `[...pool].sort(() => Math.random() - 0.5)` which is a pseudorandom sort. It results in mathematically suboptimal accumulator tickets that fail to consistently hit the target odds. |
| I18N-01 | `LiveScores.tsx:15` | 🟡 LOW | **Hardcoded English Locales**: The `STATE_CONFIG` and `getEventDisplay` functions hardcode labels like "HALF TIME", "Goal", and "Red Card", ignoring the `language` context from `useAppContext()`. |
| UX-01 | `LiveScores.tsx:188` | 🟡 LOW | **Event Parsing Fragility**: Substitution events expect `playerName` and `playerNameOut`, but Sportmonks often returns `related_player_name`. Missing fallbacks result in `? ↔ ?` being displayed. |

---

## 2. Minimax Implementation Plan

> **Instructions for Minimax**: Execute the following phases sequentially. The instructions are highly explicit. You must modify the files exactly as instructed to ensure perfect deterministic execution. Do not skip validation checks.

### Phase 1: High-Performance Backend Refactoring

**Target File:** `server.js`

**Problem:** Synchronous file reading blocks the event loop on all SSR routes.
**Action:**
1. Locate the HTML reading logic inside the catch-all SSR route (around line 615):
```javascript
        const indexPath = path.join(distPath, 'index.html');
        // Check if dist exists (important for dev environments before build)
        if (!fs.existsSync(indexPath)) {
            return res.status(404).send('Vantage AI Frontend build not found. Please run npm run build.');
        }

        let html = fs.readFileSync(indexPath, 'utf-8');
```
2. Replace it with the asynchronous equivalent:
```javascript
        const indexPath = path.join(distPath, 'index.html');
        let html = "";
        try {
            // Asynchronous read to prevent event loop blocking
            html = await fs.promises.readFile(indexPath, 'utf-8');
        } catch (err) {
            return res.status(404).send('Vantage AI Frontend build not found. Please run npm run build.');
        }
```
**Validation:** Restart the Express server. The `/` and `/predictions/YYYY-MM-DD` endpoints must return a 200 OK without blocking concurrent requests.

---

### Phase 2: Algorithmic Optimization in Ticket Wizard

**Target File:** `src/components/TicketWizard.tsx` (Note: adjust path to `components/TicketWizard.tsx` based on exact structure)

**Problem:** `TicketWizard` uses a weak random shuffle to find combinations, making results volatile and suboptimal.
**Action:**
1. Locate `findBestCombination` starting around line 51.
2. Replace the sorting and greedy loop logic with a deterministic confidence-weighted approach.
Replace these lines:
```typescript
        // Shuffle pool for variety
        const shuffled = [...pool].sort(() => Math.random() - 0.5);

        for (const match of shuffled) {
            if (currentOdds * match.odds <= target * 1.2) { // 20% margin
                bestTicket.push(match);
                currentOdds *= match.odds;

                if (currentOdds >= target * 0.9) break; // Close enough
            }
            if (bestTicket.length >= 5) break; // Limit to 5 matches Max for "Smart" feel
        }
```
With:
```typescript
        // Sort pool by confidence descending (always prioritize the best bets)
        const sortedPool = [...pool].sort((a, b) => b.confidence - a.confidence);

        for (const match of sortedPool) {
            // Prevent adding matches that push the ticket way over the target
            if (currentOdds * match.odds <= target * 1.15) { 
                bestTicket.push(match);
                currentOdds *= match.odds;
            } else if (bestTicket.length < 5 && (currentOdds * match.odds <= target * 1.3)) {
                // If we are close and just need one more, allow slight overshoot
                bestTicket.push(match);
                currentOdds *= match.odds;
                break;
            }
            if (currentOdds >= target * 0.95) break; 
            if (bestTicket.length >= 5) break; 
        }
```
**Validation:** Ensure clicking "Generate" in the UI deterministically produces tickets prioritizing high confidence picks.

---

### Phase 3: LiveScores Internationalization & Data Parsing

**Target File:** `pages/LiveScores.tsx` (or `src/pages/LiveScores.tsx`)

**Problem:** Hardcoded English labels and brittle Sportmonks event property parsing.
**Action 1 (I18N for Event Display):**
1. Change the signature of `getEventDisplay` to accept `language` and improve fallback logic:
```typescript
function getEventDisplay(ev: any, language: string): string {
  if (ev.type === 'goal' || ev.type === 'penalty') {
    const lbl = language === 'fr' ? 'But' : 'Goal';
    return `${ev.playerName || lbl}${ev.result ? ` (${ev.result})` : ''}`;
  }
  if (ev.type === 'substitution') {
    const out = ev.playerNameOut || ev.related_player_name || '?';
    return `${ev.playerName || '?'} ↔ ${out}`;
  }
  if (ev.type === 'yellow_card' || ev.type === 'red_card' || ev.type === 'yellowcard' || ev.type === 'redcard') {
    if (ev.playerName) return ev.playerName;
    if (ev.name) return ev.name;
    const isRed = (ev.type === 'redcard' || ev.type === 'red_card');
    if (language === 'fr') return isRed ? 'Carton Rouge' : 'Carton Jaune';
    return isRed ? 'Red Card' : 'Yellow Card';
  }
  if (ev.playerName) return ev.playerName;
  if (ev.name) return ev.name;
  
  const labels: Record<string, any> = {
    'var': { en: 'VAR', fr: 'VAR' },
    'event': { en: 'Event', fr: 'Événement' },
    'own_goal': { en: 'Own Goal', fr: 'Contre son camp' },
    'penalty_miss': { en: 'Penalty Miss', fr: 'Penalty raté' },
  };
  return labels[ev.type]?.[language] || labels[ev.type]?.en || 'Event';
}
```

**Action 2 (I18N for State Config):**
1. Modify `getStateConfig` to support `language`:
```typescript
function getStateConfig(short: string, language: string) {
  const key = short?.toUpperCase();
  const config = { ...(STATE_CONFIG[key] || { label: short || 'LIVE', color: 'text-green-400', bg: 'bg-green-400/15 border-green-400/30' }) };
  
  // Apply French overrides
  if (language === 'fr') {
      if (key === '1H') config.label = '1ÈRE MI-TEMPS';
      else if (key === '2H') config.label = '2ÈME MI-TEMPS';
      else if (key === 'HT') config.label = 'MI-TEMPS';
      else if (key === 'ET') config.label = 'PROLONGATION';
      else if (key === 'PEN') config.label = 'TIRS AU BUT';
      else if (key === 'FT') config.label = 'TERMINÉ';
  }
  return config;
}
```

**Action 3 (Component Updates):**
1. In `LiveMatchCard`, destructure `language` from a prop or context. Since `LiveMatchCard` doesn't use `useAppContext`, pass `language` to it from `LiveScores`.
2. Update the calls in `LiveMatchCard`: `const stateConf = getStateConfig(match.stateShort, language);` and `<span className="text-gray-300 font-medium truncate">{getEventDisplay(ev, language)}</span>`.
3. In `LiveScores`, pass `language={language}` to `<LiveMatchCard key={m.id} match={m} idx={idx} language={language} />`. Update `LiveMatchCard` interface accordingly: `const LiveMatchCard: React.FC<{ match: LiveMatch; idx: number; language: string }>`.

**Validation:** Switch the app language to French. Verify that "HALF TIME" correctly becomes "MI-TEMPS" and goals show as "But". Substitution events must display `Player In ↔ Player Out`.

---

## 3. Final Verification

After all changes, Minimax must ensure that:
1. `npm run build` succeeds with 0 TypeScript compilation errors.
2. The Node.js application `node server.js` boots gracefully without warnings.
3. The Vite development server renders Live Scores and the Ticket Wizard components correctly in both languages.
