# Python dependencies for the Vantage AI Quant Engine
# Install with: pip install -r backend/quant/requirements.txt

# Core scientific computing
scipy>=1.11.0          # Poisson PMF calculations
numpy>=1.24.0          # Array operations

# HTTP requests (Sportmonks API + free data stack calls)
requests>=2.31.0

# Understat xG data (free data stack)
understatapi>=0.4.0

# Firebase Admin (for Firestore read/write from Python)
firebase-admin>=6.2.0
google-cloud-firestore>=2.11.1

# gRPC with pre-built wheels (avoids native compilation on Railway)
grpcio>=1.59.0

# Utilities
python-dateutil>=2.8.2 # Date parsing helpers

# International data (FBref/soccerdata)
soccerdata>=1.6.0

# football-data-api PyPI package (cleaner boilerplate request handling)
football-data-api>=0.1.0

# Data parsing
pandas>=2.0.0
lxml>=4.9.0
