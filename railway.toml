[build]
builder = "NIXPACKS"
buildCommand = "npm install && pip3 install --no-cache-dir --break-system-packages -r backend/quant/requirements.txt"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"

[env]
PORT = "8080"

[deploy.healthcheck]
path = "/health"
timeout = 30